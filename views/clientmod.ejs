<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Autókölcsönző</title>

    <link href="/css/bootstrap.css" rel="stylesheet">
    <link href="/css/bootstrap-theme.css" rel="stylesheet">
    <link href="/css/design.css" rel="stylesheet">
</head>
<body>

<div class="jumbotron text-center">
    <h1>Kovács járműkölcsönző</h1>
    <p>Vidd el a kocsit, de azért hozd is vissza!</p>
</div>

<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="#">Járműkölcsönző</a>
        </div>
        <ul class="nav navbar-nav">
            <li <% if(page === 'freecars') {%>class="active" <%}%>><a href="/freecars">Szabad autók</a></li>
            <li <% if(page === 'occupiedcars') {%>class="active" <%}%>><a href="/occopiedcars">Foglalt autók</a></li>
            <li <% if(page === 'clients') {%>class="active" <%}%>><a href="/clients">Kölcsönzők</a></li>
        </ul>
    </div>
</nav>

<div class="container">
    <div class="row">
        <div class="col-xs-12 text-center">
            <h2>Kölcsönző részletei</h2>
        </div>
        <div class="col-md-push-3 col-md-6 col-xs-12">
            <form method="post">
                <div class="form-group">
                    <label for="nationalId">Személyi szám</label>
                    <input name="nationalId" type="text" class="form-control" id="nationalId" value="<%= client.nationalId%>" placeholder="Személyi szám">
                </div>
                <div class="form-group">
                    <label for="name">Név</label>
                    <input name="name" type="text" class="form-control" id="name" value="<%= client.name %>" placeholder="Név">
                </div>
                <div class="form-group">
                    <label for="address">Lakcím</label>
                    <input name="address" type="text" class="form-control" id="address" value="<%= client.address%>" placeholder="Lakcím">
                </div>
                <div class="form-group">
                    <label for="phone">Telefon szám</label>
                    <input name="phone" type="text" class="form-control" id="phone" value="<%= client.phone%>" placeholder="Telefon szám">
                </div>
                <div class="container">
                    <label>Jogosítvány típus</label>
                        <label class="radio-inline">
                            <input <% if(client.licence == 'B') { %> checked="checked" <% } %> type="radio" name="car" value="B">Személygépjármű
                        </label>
                        <label class="radio-inline">
                            <input <% if(client.licence == 'C') { %> checked="checked" <% } %> type="radio" name="car" value="C">Haszongépjármű
                        </label>
                        <label class="radio-inline">
                            <input <% if(client.licence == 'A') { %> checked="checked" <% } %> type="radio" name="car" value="A">Motorkerékpár
                        </label>
                </div>
                <a href="/clients/mod/<%= client._id%>"><button class="btn btn-danger">Módosítás</button></a>
                <a href="/clients" class="btn btn-default">Mégsem</a>
            </form>

        </div>
    </div>
</div>

<script src="/js/jquery-3.1.1.min.js"></script>
<script src="/js/bootstrap.js"></script>
</body>
</html>
